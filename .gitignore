# Claude Code — 개인/런타임 상태 (git 제외)
.claude/settings.local.json
.omc/state/
.omc/sessions/
.omc/project-memory.json
.omc/prd.json
.omc/progress.txt
*_codex.md
DB_VOICE_STRUCTURE.md
# OMC가 어떤 작업 디렉토리에서든 만들 수 있는 런타임 폴더 (예: .claude/skills/.omc/)
**/.omc/state/
**/.omc/sessions/
**/.omc/project-memory.json
**/.omc/prd.json
**/.omc/progress.txt

# 시크릿 / 환경 변수
.env
.env.local
.env.*.local

# OS
.DS_Store
*.swp
*~
Thumbs.db

# Editor (개인 워크스페이스 설정)
.vscode/
**/.vscode/
.claude/worktrees/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
*.egg-info/
dist/
build/

# ROS2 (미래)
ros2_ws/build/
ros2_ws/install/
ros2_ws/log/
# colcon을 레포 루트에서 잘못 실행했을 때 생기는 디렉토리
install/
log/
*.bag

# DB 운영 데이터 (git 제외, 별도 백업)
*.db
*.sqlite
*.sqlite3
*.db-wal
*.db-shm

# 모델 가중치 (용량 초과)
*.pt
*.pth
*.onnx
*.bin
*.safetensors

# 로그
*.log

# 캘리브레이션 결과 (장비별 로컬 값, git 제외)
config/hand_eye.yaml
config/c270_hand_eye.yaml

# 캘리브레이션 샘플 이미지 (로컬 수집 데이터, git 제외)
scripts/samples/
scripts/samples_c270/
scripts/samples_c270_calib/
scripts/samples_c270_handeye/

# 캘리브레이션 샘플 이미지 (로컬 수집 데이터, git 제외)
scripts/samples/
scripts/samples_c270/
scripts/samples_c270_calib/
scripts/samples_c270_handeye/

# verify_camera.py 출력 이미지 (로컬 검증용)
verify_rgb.png
verify_depth_colormap.png

# 데이터셋 이미지·라벨 (용량 초과 — Roboflow 관리)
datasets/tools/**/images/
datasets/tools/**/labels/
datasets/tools/**/train/
datasets/tools/**/valid/
datasets/tools/**/test/
datasets/tools/top_view/auto_labels/

# SAM 변환 결과물 (246MB, Roboflow 업로드 완료 — 로컬 전용)
Final_project_coco_segmentation/

# 세그멘테이션 학습 데이터셋 이미지 (Roboflow 관리 — 로컬 전용, data.yaml만 추적)
datasets/tools/top_view_seg/train/
datasets/tools/top_view_seg/valid/
datasets/tools/top_view_seg/test/
datasets/tools/top_view_seg/README*.txt

# 핸드아이 캘리브레이션 수집 데이터 (로컬 전용)
scripts/*.npz

# YOLO 학습 결과물
runs/

# model_library — weights·results는 로컬 전용 (model_info.yaml·README.md는 추적)
ros2_ws/src/vision/model_library/**/weights/
ros2_ws/src/vision/model_library/**/results/

# model_registry — 바이너리/결과물 로컬 전용 (models.yaml·README.md는 추적)
ros2_ws/src/vision/model_registry/weights/
ros2_ws/src/vision/model_registry/results/

# 스냅샷 수집 raw 데이터 (Roboflow 업로드 완료 — 로컬 전용)
scripts/infer_snapshots/*.jpg
scripts/infer_snapshots/auto_labels/
