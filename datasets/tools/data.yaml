---
# 클래스 정의 참조용 — 실제 학습은 top_view/data.yaml · gripper/data.yaml 사용
# 참조: config/vision.yaml class_names, config/toolbox.yaml
# 인덱스 순서 변경 시 config/vision.yaml class_names + 두 하위 data.yaml 동시 갱신 필수
# tool_id는 robot_arm.db tools 테이블과 일치해야 함

path: datasets/tools   # 프로젝트 루트 기준 상대경로
train: images/train
val: images/val

nc: 6
names:
  0: screwdriver
  1: utility_knife
  2: ratchet_wrench
  3: multi_tool
  4: spanner_16mm
  5: socket_19mm
