whisper_node:
  ros__parameters:
    whisper_no_speech_threshold: 0.6
    whisper_logprob_threshold: -1.0
    whisper_compression_ratio_threshold: 2.4
    reject_hallucinated_transcripts: true

rule_intent_node:
  ros__parameters:
    require_wake_word: false
    wake_words:
      - 로봇
